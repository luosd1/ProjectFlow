# T23.A — 冷启动 + 规划流测试

## 你的任务

从零开始走完：注册 → 创建工作区 → 填写成员资料 → 新建项目 → Agent 方向澄清 → 阶段规划 → 任务拆解。

验证一个完全不了解项目的新用户，能否无文档走通整个流程，以及 Agent 输出质量是否达标。

---

## 环境准备

### 1. 启动后端

```bash
cd backend
.venv\Scripts\Activate.ps1
python -m uvicorn app.main:app --reload --port 8000
```

确认 `http://localhost:8000/docs` 可访问。

### 2. 启动前端

新开一个终端：

```bash
cd frontend
npm install   # 首次需要
npm run dev
```

确认 `http://localhost:3000` 可访问。

### 3. 确认 LLM 配置

打开 `backend/.env`，确认：

- **第一轮（mock 模式）**：`LLM_PROVIDER=mock`
- **第二轮（真实 LLM）**：`LLM_PROVIDER=openai`，`LLM_API_KEY=你的key`，`LLM_MODEL=gpt-4o-mini`

修改 `.env` 后后端会自动重载。

### 4. 清空旧数据

在浏览器访问 `http://localhost:8000/docs`，找到 `POST /api/seed/reset`，点击 "Try it out" → "Execute"。或用前端首页的"重置演示"按钮。

---

## 测试步骤

### A1 — 首页

1. 打开 `http://localhost:3000`
2. 清除 localStorage（DevTools → Application → Local Storage → Clear）

**验证：**
- [ ] 看到欢迎页，标题含 "ProjectFlow"
- [ ] 有"开始使用"按钮
- [ ] 有"加载演示数据"按钮
- [ ] 无报错、无白屏

**截图：** 首页全貌

---

### A2 — 账号设置

1. 点击"开始使用"
2. 进入 `/onboarding` 页面

**验证：**
- [ ] 页面标题"账号设置"
- [ ] 有输入框填写用户名（display_name）
- [ ] 可能有演示身份选择区域
- [ ] 使用 shadcn 组件（非原生 HTML input）

**截图：** 账号设置页

---

### A3 — 创建用户

1. 输入你的名字（如"测试A"）
2. 提交

**验证：**
- [ ] 创建成功，页面跳转
- [ ] 有 loading 状态（转圈动画）
- [ ] 无报错

---

### A4 — 成员资料填写

1. 进入 `/onboarding/profile` 页面
2. 填写以下信息：
   - **技能**：添加 2-3 项（如"Python: 3"、"React: 2"、"设计: 1"），level 为 1-5
   - **可用时间**：如 15（小时/周）
   - **角色偏好**：如"后端开发"
   - **意向**：如"想做 AI 相关功能"
   - **限制**：如"周三晚上没空"

**验证：**
- [ ] 向导步骤清晰，每步有标题说明
- [ ] 技能输入支持添加多项
- [ ] 每步有 loading/success 状态反馈
- [ ] 提交后数据保存成功

**截图：** 成员资料填写页

---

### A5 — 创建工作区

1. 进入 `/workspaces/new`
2. 填写工作区名称（如"测试团队"）
3. 提交

**验证：**
- [ ] 创建成功，跳转到工作台 `/workspaces/{id}`
- [ ] 导航栏出现"工作台"入口
- [ ] 工作台显示工作区名称
- [ ] 你出现在成员列表中，角色为 owner

**截图：** 工作台页面

---

### A6 — 邀请成员

1. 在工作台页面，找到邀请成员入口
2. 邀请至少 3 位成员（输入名字即可，如"成员B"、"成员C"、"成员D"）

**验证：**
- [ ] 邀请流程顺畅
- [ ] 被邀请人出现在成员列表
- [ ] 邀请状态显示为 pending 或 accepted

**截图：** 邀请面板

---

### A7 — 为每个成员填写 profile

1. 为每位被邀请的成员填写成员资料（技能/时间/意向/限制）
2. 确保成员之间有差异化（如一人偏前端、一人偏后端、一人偏设计）

**验证：**
- [ ] 每个成员的资料都能保存
- [ ] 工作台成员列表显示 profile 信息（技能/时间等）
- [ ] 数据与填写内容一致

---

### A8 — 新建项目

1. 点击导航栏"新建项目"或工作台的创建项目入口
2. 进入 `/projects/new`
3. 填写：
   - **项目名称**：如"校园二手交易平台"
   - **项目想法**：如"帮助大学生在校内安全便捷地交易二手物品，支持实名认证和信用评价"
   - **截止日期**：选择一个未来日期
   - **交付物**：如"可演示的 Web 应用 + 项目文档"

**验证：**
- [ ] 项目创建成功，跳转到项目仪表盘 `/projects/{id}`
- [ ] 仪表盘显示项目名称、想法、截止日期
- [ ] 项目状态显示"草稿"或"进行中"
- [ ] 面包屑导航：工作台 → 项目名

**截图：** 项目仪表盘初始状态

---

### A9 — 添加项目资源

1. 在项目仪表盘中，找到资源输入面板
2. 添加 1-2 个资源：
   - 文本笔记：如"训练营要求：必须使用 Next.js + FastAPI"
   - 链接：如 `https://ui.shadcn.com`

**验证：**
- [ ] 资源添加成功
- [ ] 资源列表显示标题和类型
- [ ] 链接类型可点击

---

### A10 — Agent 方向澄清

1. 在项目仪表盘的"Agent 操作"区域，找到"规划"阶段
2. 点击"澄清方向"按钮（推荐操作，按钮为深色高亮）

**验证：**
- [ ] 按钮点击后显示 loading（"运行中..."）
- [ ] 等待后，"Agent 提案"面板出现新提案，类型为"方向卡"
- [ ] 提案状态为"待确认"，有"待确认"Badge
- [ ] Agent 操作区域显示成功消息（绿色）

**截图：** Agent 提案面板 - 方向卡

---

### A11 — 查看方向卡内容

1. 点击方向卡提案展开
2. 逐项检查内容

**验证：**
- [ ] 包含"核心问题"（problem）
- [ ] 包含"目标用户"（users）
- [ ] 包含"核心价值"（value）
- [ ] 包含"交付物"列表（deliverables）
- [ ] 包含"边界"列表（boundaries）
- [ ] 包含"风险"列表（risks）
- [ ] 包含 reason（Agent 推理说明）
- [ ] **真实 LLM 模式**：内容是否针对你的项目想法，而非泛泛而谈
- [ ] **真实 LLM 模式**：是否编造了不存在的成员或任务

**截图：** 方向卡展开内容

---

### A12 — 确认方向卡

1. 点击"确认应用"按钮

**验证：**
- [ ] 提案状态变为"已确认"
- [ ] 仪表盘"方向卡面板"出现方向卡内容
- [ ] 项目 header 区域"推荐下一步"更新为"生成阶段计划"
- [ ] Agent 操作区域"规划"阶段仍高亮

---

### A13 — Agent 阶段规划

1. 点击"生成阶段计划"按钮

**验证：**
- [ ] loading 状态正常
- [ ] "Agent 提案"面板出现"阶段计划"提案
- [ ] 提案包含多个阶段（通常 3 个）

**截图：** 阶段计划提案

---

### A14 — 查看并确认阶段计划

1. 展开阶段计划提案
2. 检查每个阶段包含：名称、目标、起止日期、交付物
3. 点击"确认应用"

**验证：**
- [ ] 每个阶段有 name、goal、start_date、end_date、deliverable
- [ ] 阶段时间范围合理（不重叠、在截止日期内）
- [ ] 确认后，"阶段计划看板"出现阶段卡片
- [ ] 第一个阶段状态为 active，其余为 pending
- [ ] "推荐下一步"更新为"分解任务"
- [ ] **真实 LLM 模式**：阶段划分是否合理，是否针对项目特点

**截图：** 阶段计划看板

---

### A15 — Agent 任务拆解

1. 点击"分解任务"按钮

**验证：**
- [ ] loading 状态正常
- [ ] "Agent 提案"面板出现"任务分解"提案
- [ ] 提案包含多个任务（通常 3 个）

**截图：** 任务分解提案

---

### A16 — 查看并确认任务拆解

1. 展开任务分解提案
2. 检查每个任务包含：标题、描述、优先级（P0/P1/P2）、预估时间
3. 点击"确认应用"

**验证：**
- [ ] 每个任务有 title、description、priority、estimated_hours
- [ ] 优先级分布合理（至少 1 个 P0）
- [ ] 确认后，"任务拆解看板"出现任务卡片
- [ ] 任务关联到当前 active 阶段
- [ ] "推荐下一步"更新为"推荐分工"
- [ ] 仪表盘指标卡更新：P0 待处理数、分工覆盖率、活跃行动卡数
- [ ] **真实 LLM 模式**：任务是否针对阶段目标拆解，优先级是否合理

**截图：** 任务拆解看板 + 仪表盘指标

---

### A17 — 拒绝提案（反向测试）

1. 重置数据（`POST /api/seed/reset`）
2. 重新走 A8-A10，生成方向卡
3. 这次点击"拒绝"而非"确认"

**验证：**
- [ ] 提案状态变为"已拒绝"
- [ ] 方向卡面板不出现内容
- [ ] 可以重新运行"澄清方向"生成新提案
- [ ] 页面无崩溃

---

### A18 — Agent 失败 fallback（mock 模式特有）

在 mock 模式下 Agent 通常不会失败。如果你能触发真实 LLM 超时（如设置 `LLM_TIMEOUT_SECONDS=1` 后运行 Agent），观察：

**验证：**
- [ ] 超时后显示错误消息（红色提示）
- [ ] 页面不崩溃
- [ ] 可以重新运行 Agent 操作
- [ ] 如果 fallback 触发，输出有合法默认值（无 None/空字段）

---

## 横向评估清单

每个步骤都要关注以下维度，发现问题记录到反馈表。

### Agent 输出质量

- [ ] 方向卡内容是否针对项目想法（非泛泛而谈）
- [ ] 阶段划分是否合理（数量、时间、目标）
- [ ] 任务优先级是否恰当（P0 真的是最重要的吗）
- [ ] 每个输出是否都有 reason
- [ ] Agent 是否编造了不存在的成员/任务/阶段

### 交互体验

- [ ] 每步操作后是否有 loading 状态
- [ ] 确认/拒绝后页面是否自动更新
- [ ] 面包屑导航是否正确
- [ ] "推荐下一步"推断是否正确
- [ ] 中文是否统一（无英文混入）
- [ ] 表单是否使用 shadcn 组件

### 数据一致性

- [ ] 确认后刷新页面（F5），数据是否还在
- [ ] 方向卡字段是否完整（problem/users/value/deliverables/boundaries/risks）
- [ ] 阶段字段是否完整（name/goal/start_date/end_date/deliverable）
- [ ] 任务字段是否完整（title/description/priority/estimated_hours）

### UI 状态覆盖

- [ ] 每个区域是否有 empty 状态（虚线边框 + 提示文字）
- [ ] Agent 运行中是否有 loading 动画
- [ ] Agent 失败是否有 error 提示
- [ ] Agent 成功是否有 success 提示

---

## 反馈格式

发现问题请按以下格式记录：

```
【A-步骤号】【现象】描述你看到了什么
【期望】描述你认为应该怎样
【严重度】❌ 阻塞 / ⚠️ 体验 / 💡 优化
【截图】附截图
```

示例：

```
【A-10】【现象】点击"澄清方向"后按钮变为"运行中..."，但 30 秒后仍无响应
【期望】应在 120 秒内返回结果，或显示超时提示
【严重度】❌ 阻塞
【截图】
```

---

## 测试节奏

| 阶段 | 内容 | 预计时长 |
|------|------|---------|
| 准备 | 配环境、跑通 mock | 20min |
| 第一轮 | mock 模式走完 A1-A18 | 40min |
| 第二轮 | 真实 LLM 走完 A1-A16，重点评估 Agent 输出质量 | 60min |
| 记录 | 整理反馈清单 | 20min |
