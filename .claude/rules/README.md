# Project Rules

ProjectFlow working rules are maintained in the repository root:

- `AGENTS.md`
- `CLAUDE.md`

This directory exists so CCPM validation can recognize the local rules location without duplicating or drifting from the root instructions.
